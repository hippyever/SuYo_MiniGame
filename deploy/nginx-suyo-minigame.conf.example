# Replace play.example.com and admin.example.com with your real domains.
# After installing this file, run:
#   sudo certbot --nginx -d play.example.com -d admin.example.com

server {
    listen 80;
    listen [::]:80;
    server_name play.example.com admin.example.com;

    client_max_body_size 320m;
    proxy_read_timeout 600s;
    proxy_send_timeout 600s;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
